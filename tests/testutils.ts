import { CommandInteraction } from "discord.js";
import MockDiscord from "./mockDiscord";

export const defaultConfig = {
  id: '11',
  lang: 'en',
  prefix: '.',
  almanaxChannel: 'almanax',
  partyChannel: 'listagem-de-groupos',
  buildPreview: 'enabled'
}
